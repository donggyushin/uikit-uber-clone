name: Swift

on:
  push:
    branches: [ main, develop, feature/* ]
  pull_request:
    branches: [ main, develop, feature/* ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build and test ( iOS 15.0)
      run: xcodebuild -project uikit-uber-clone/uikit-uber-clone.xcodeproj -scheme uikit-uber-clone -destination 'platform=iOS,name=iPhone 12 Pro' CODE_SIGNING_ALLOWED=NO IPHONEOS_DEPLOYMENT_TARGET=13.0 test
